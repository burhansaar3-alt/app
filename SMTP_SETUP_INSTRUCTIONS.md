# 📧 تعليمات إعداد إرسال البريد الإلكتروني

## الوضع الحالي ✅
- النظام يعمل بشكل كامل
- الرمز يظهر تلقائياً في الواجهة (وضع التطوير)
- الرمز يُحفظ في قاعدة البيانات بشكل آمن

## لإضافة إرسال البريد الإلكتروني الفعلي 📬

### الخيار 1: Gmail SMTP (مجاني)

#### الخطوة 1: إنشاء App Password في Gmail
1. اذهب إلى حسابك في Google: https://myaccount.google.com/
2. اذهب إلى **Security** (الأمان)
3. فعّل **2-Step Verification** إذا لم يكن مفعلاً
4. بعد التفعيل، ابحث عن **App passwords** (كلمات مرور التطبيقات)
5. اختر **Mail** و **Other** (أدخل اسم: Souq Syria)
6. انسخ كلمة المرور التي تظهر (16 حرف)

#### الخطوة 2: أضف البيانات إلى .env
افتح الملف `/app/backend/.env` وأضف:

```env
# SMTP Configuration for Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password
SMTP_FROM=your-email@gmail.com
ENVIRONMENT=production
```

#### الخطوة 3: أعد تشغيل Backend
```bash
sudo supervisorctl restart backend
```

---

### الخيار 2: خدمات بريد مجانية أخرى

#### SendGrid (100 email/day مجاناً)
1. سجل في: https://sendgrid.com/
2. احصل على API Key
3. أضف إلى `.env`:
```env
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=your-sendgrid-api-key
SMTP_FROM=your-verified-email@yourdomain.com
ENVIRONMENT=production
```

#### Mailgun (مجاني حتى 5000 email/شهر)
```env
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=postmaster@your-domain.mailgun.org
SMTP_PASSWORD=your-mailgun-password
SMTP_FROM=noreply@your-domain.com
ENVIRONMENT=production
```

---

## اختبار الإرسال 🧪

بعد إضافة بيانات SMTP:

1. اذهب إلى صفحة تسجيل الدخول
2. اضغط "نسيت كلمة المرور؟"
3. أدخل بريدك الإلكتروني
4. اضغط "إرسال رمز التحقق"
5. افتح بريدك الإلكتروني وابحث عن الرسالة

---

## ملاحظات مهمة ⚠️

### الوضع الحالي (Development)
- ✅ الرمز يظهر في الواجهة مباشرة
- ✅ مناسب للتطوير والاختبار
- ✅ لا يحتاج بيانات SMTP

### بعد إضافة SMTP (Production)
- ✅ الرمز يُرسل عبر البريد الإلكتروني
- ✅ الرمز لا يظهر في الواجهة (للأمان)
- ✅ تجربة مستخدم احترافية

---

## استكشاف الأخطاء 🔧

### إذا لم يصل البريد:
1. تحقق من بيانات SMTP في `.env`
2. تحقق من logs: `tail -f /var/log/supervisor/backend.err.log`
3. تحقق من صندوق Spam في البريد
4. تأكد من أن Gmail App Password صحيح (16 حرف بدون فراغات)

### للتحقق من الإرسال:
```bash
# شاهد logs الـ backend
tail -f /var/log/supervisor/backend.out.log | grep -i "email"
```

---

## الدعم 💬

إذا واجهت أي مشكلة:
- تحقق من الـ logs
- تأكد من صحة بيانات SMTP
- جرب خدمة SMTP أخرى إذا لم يعمل Gmail

---

© 2025 سوق سوريا - جميع الحقوق محفوظة
